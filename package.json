{
  "name": "ci-labs",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"Build complete!\" && mkdir -p dist && cp index.js dist/",
    "test": "node test.js"
  },
  "dependencies": {
    "lodash": "^4.18.1",
    "pg": "^8.23.0"
  }
}
